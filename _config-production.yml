# In order to build for production run:
# $ jekyll build --config _config.yml, _config-production.yml
#
# URL of static site
url: "https://connect.linaro.org"
# Destination folder
destination: production.connect.linaro.org
# Production boolean used in layouts / includes
production: true
# GitHub branch for netlify CMS config.yml file.
branch: master
# Google Analytics / Tag Manager configuration
google_analytics:
  enabled: false
  code: GTM-T9KN28Q
